# Seed Research

No CVE, GHSA, advisory id, package version, or external vulnerability family seed was provided by the user.

Local seed hints used:
- User explicitly asked for repository security scan.
- Current working tree shows deleted `data/raw` files; treated as operational build-data risk, not a vulnerability seed.
- Runtime surfaces derived from repository files: `frontend/`, `functions/api/`, `backend/server.js`, `database/schema.sql`, `scripts/`, `wrangler.toml`.

Network/advisory research: not applicable. No advisory identifiers were supplied.
