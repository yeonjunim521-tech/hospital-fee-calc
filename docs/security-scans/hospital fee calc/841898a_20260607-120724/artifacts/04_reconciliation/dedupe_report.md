# Dedupe Report

No cross-file dedupe merged independently reachable reportable instances.

- CF-001 remains one finding because search-stats read and delete-log mutation share the same missing admin-auth root boundary and same `/api/admin` administrative surface.
- CF-002 remains one finding because the root class is repeated DOM-safe-output failure in the same final render layer; the affected lines are listed separately in the final report.
- CF-003 remains deferred separately.
- CF-004 remains suppressed separately.
