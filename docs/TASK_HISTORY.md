# 작업 기록

이 파일은 `TASKS.md`에서 빠진 이전 작업 기록을 보관한다.

## 관리자 검색 통계 실제 검색어 복구

- `aggregate` 고정값 대신 개인정보 필터를 통과한 실제 검색어·검색 결과·경로를 저장하도록 복구했다.
- 클릭한 의료 항목 코드·이름을 저장하고 기존 `aggregate` 행은 관리자 통계에서 제외했다.
- 주민등록번호·전화번호·이메일 패턴 차단, 분석 동의, 30일 보유, IP·User-Agent 미수집 원칙을 유지했다.
- Node.js·TypeScript 회귀 검증은 통과했으며 원 작업 환경에서는 Chrome 실행 파일이 없어 화면 검증을 완료하지 못했다.

## Kakao AdFit 설치와 동의 분리

- 법적 위험을 줄이면서 정적 안내 페이지에서 AdFit을 즉시 호출하고 Google Analytics 동의만 별도로 유지했다.
- 광고 동의 항목 제거, 정적 안내 페이지 7곳 설치, 개인정보 처리 안내와 자동 검증을 완료했다.
- 메인 계산기, 사용자 입력, 계산 결과 화면에는 광고를 배치하지 않았다.

## main 대상 PR 생성 및 GitHub 동기화

- commit(커밋): `5310e06 feat: add calculation logs and result gate`
- push(푸시): `origin/feature-search-log`
- PR(변경 요청): GitHub에서 생성 후 `main`에 merge(병합)
- 로컬 `main`은 GitHub 최신 `origin/main` 기준으로 덮어쓰기 완료

## TASKS 최근 3개 작업 유지 규칙 적용

- 기존에는 최근 3개 작업만 `TASKS.md`에 남기도록 변경했다.
- 이번 작업에서 다시 변경: `TASKS.md`에는 마지막 작업 하나만 남기고, 이전 기록은 이 파일에 보관한다.

## 로컬 관리자 토큰 임시 파일 삭제 확인

- `.dev.vars` 파일은 프로젝트 폴더에 존재하지 않았다.
- 관리자 토큰값이 들어간 로컬 환경 파일도 발견되지 않았다.
- `.gitignore`에 `.dev.vars`, `.env`, `.env.*` 제외 설정을 유지했다.
## 병원 종별가산 항목 자동 산정 표시

- 목표: 상급종합병원가산 등 병원 등급별 가산이 자동 계산되도록 정리하고, 결과 상세 내역에 가산 항목을 별도로 표시.
- 확인: `DB.HOSPITAL_CLASS`에 의원 15%, 병원 20%, 종합병원 25%, 상급종합병원 30% 가산율 존재.
- 변경: 급여 항목의 가산 전/후 차이를 계산 상세와 결과 요약에서 확인 가능하게 처리.
- 검증: `node --check frontend\assets\js\script.js`, `node tests\test_runner.js`, `git diff --check` 통과 기록.

## 구글 애드센스 소유권 확인 코드 삽입

- 목표: AdSense(애드센스) 사이트 검토를 위해 배포되는 HTML 페이지의 `<head>`에 애드센스 코드 snippet(스니펫)을 삽입하고 `ads.txt`를 실제 publisher id(게시자 ID)로 갱신.
- 범위: 공개 HTML 페이지에 애드센스 스크립트 삽입, `ads.txt` publisher id 갱신, 삽입 검증 및 Git 상태 확인.
- 메모: 애드센스 client id는 `ca-pub-1927730301151401`.

## 관리자 검색 통계 대시보드 시안 및 기능 추가

- 목표: 관리자 페이지에서 방문자 검색 통계를 기간별로 확인하고, 검색 결과가 없어 새로 추가한 항목도 확인할 수 있는 시안용 HTML과 API 기능 추가.
- 완료: 오늘, 24시간, 3일, 7일, 30일, 90일 기간 필터.
- 완료: 시안 2번 기준 관리자 대시보드 HTML.
- 완료: 무결과 후 추가한 항목 저장/조회 기능.
- 검증: `node tests/test_runner.js`, HTML 내장 스크립트 `node --check`, `git diff --check`.
- 검증 못함: 로컬 `tsc` 부재로 TypeScript 타입 체크 미실행.

## 무결과 검색어 실제 DB 반영·주간 자동 점검

- 승인된 의료 항목을 D1 검색 DB와 공개 계산기에 반영했다.
- 최근 7일 무결과 검색어를 매주 월요일 오전 9시(KST)에 후보화하도록 구성했다.
- 완료 후보 키워드와 하지 MRI 부위별 공식 코드·가격 preset을 추가했다.
- 회귀 테스트, 브라우저 확인, 원격 D1 적용과 배포까지 완료했다.

## MEDICost v3.0 메인 페이지 고도화

- 밝고 신뢰감 있는 소개형 메인과 3단계 계산 흐름을 구축했다.
- 계산 데이터 지연 로드, 분석·광고 동의 후 로드, 지원 페이지 복구를 완료했다.
- 기존 계산 공식, 의료 데이터, API, D1 schema를 유지했다.
- 계산·검색 회귀 테스트와 실제 Chrome 핵심 흐름을 확인했다.

## MEDICost 빠른 계산 사용성 개선 설계

- 빠른 결과 우선 흐름과 고급 항목 분리 방향을 확정했다.
- 설계 문서: `docs/superpowers/specs/2026-07-11-medicost-fast-calculation-design.md`.
- 이번 의료수가·마취 계산 작업에서 필요한 부분만 이어서 구현한다.

## 의료수가 검색·마취 자동 산정 운영 반영

- 관리자 문구·키워드 줄바꿈·기본 7일을 확인했다.
- MRI·엑스레이·골절·마취 공식 수가 406개와 검색 별칭 2,216개를 운영 D1에 반영했다.
- 안전하게 연결 가능한 승인대기 4건을 실제 수가 코드로 승인했다.
- 수술 마취 회차·유형·시간·연령·진정 여부 예상 계산을 추가했다.
- 자동 테스트·Chrome 검증·commit·push·Cloudflare Pages 운영 배포를 완료했다.

## Kakao AdFit 설치와 동의 분리

- 개인정보보호법·개인정보위 정책·AdFit 약관을 확인했다.
- 광고 동의 항목을 제거하고 Google Analytics 분석 동의를 분리했다.
- 정적 안내 페이지 7곳에 Kakao AdFit 스크립트를 직접 설치했다.
- 개인정보 처리 안내와 관련 테스트를 갱신했다.
- Node.js와 실제 Chrome 자동 검증을 완료했다.
- 메인 계산기·사용자 입력·계산 결과 화면에는 광고를 배치하지 않았다.
## 2026-08-31 AdSense 승인 전 AdFit·쿠팡 배너 수익화 및 모바일 노출 개선

- Kakao AdFit 광고단위 2개와 쿠팡 다이나믹 배너 3개를 별도 worktree에서 연결했다.
- PR #11을 원격 `main`에 병합하고 Cloudflare Pages 운영 배포까지 완료했다.
- 후속 운영 확인에서 PC breakpoint, AdFit 호출 위치, 일반 쿠팡 URL 문제가 발견되어 현재 수정 작업으로 이관했다.
