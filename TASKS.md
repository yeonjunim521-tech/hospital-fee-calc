# 작업 목록

## 운영 규칙

- 작업 목록 파일은 `TASKS.md` 하나만 사용한다.
- `TASKS.md`에는 현재 작업 하나만 기록한다.
- 이전 작업 기록은 `docs/TASK_HISTORY.md`에 보관한다.
- 상태 표기: `[ ]` 미완료, `[/]` 진행 중, `[x]` 완료

## 현재 작업: AdFit·쿠팡 반응형 운영 버그 수정

목표: PC가 모바일 1열로 접히는 문제, 화면 밖 AdFit과 중복 스크립트 호출, 모바일 breakpoint 충돌, 검증되지 않은 일반 쿠팡 URL을 제거한다. 실제 파트너스 추적 배너만 사용하고 계산 공식·데이터·API·D1은 변경하지 않는다.

## 작업 범위

- [x] 운영 390·768·1000·1280·1440px 증상 재현
- [x] 실패 회귀 테스트 작성 후 원인별 최소 수정
- [x] 화면별 AdFit 한 단위 지연 호출과 PC 2열·모바일 1열 분리
- [x] 일반 쿠팡 URL 제거와 결과 안내 다이나믹 배너 연결
- [x] 전체 자동 테스트와 로컬 모바일·PC 실화면 QA
- [x] 사용자 승인 확보 및 배포용 변경 세트 준비

## 진행 메모

- 768~1279px는 2열 PC/태블릿, 767px 이하는 1열 모바일, 1280px 이상은 좌우 레일을 사용한다.
- 홈 AdFit은 현재 화면에 맞는 단위 하나만 DOM에 만들고 광고 위치가 가까워질 때 공식 스크립트를 한 번 호출한다.
- 열린 화면을 1279px 경계 너머로 리사이즈해도 활성 AdFit 단위를 하나만 다시 선택한다.
- 쿠팡 수익 연결은 `trackingCode=AF2104018`이 들어간 다이나믹 배너만 인정한다. 직접 만든 일반 `www.coupang.com?lptag=...` 링크는 제거한다.
- `.codex-progress/`, `.omo/`, `_tmp_*.py`는 커밋하지 않는다.
