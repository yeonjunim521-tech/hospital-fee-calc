# 작업 목록

## 운영 규칙

- 작업 목록 파일은 `TASKS.md` 하나만 사용한다.
- `TASKS.md`에는 현재 작업 하나만 남긴다.
- 이전 작업 기록은 `docs/TASK_HISTORY.md`에 보관한다.
- 상태 표기: `[ ]` 미완료, `[/]` 진행 중, `[x]` 완료

## 현재 작업: 병원 종별가산 항목 자동 산정 표시

목표: 상급종합병원가산 등 병원 등급별 가산이 자동 계산되도록 명확히 정리하고, 결과 상세 내역에 가산 항목을 별도로 표시한다.

## 작업 범위

- [x] 기존 가산 계산 로직 확인
- [x] 병원 등급별 가산율/가산명 helper(도우미 함수) 추가
- [x] 급여 항목의 가산 전/후 금액 차이를 자동 산정
- [x] 결과 상세 내역에 종별가산 항목 표시
- [x] 문법/test(테스트) 검증
- [/] commit(커밋), push(푸시)

## 진행 메모

- 현재 `DB.HOSPITAL_CLASS`에 의원 15%, 병원 20%, 종합병원 25%, 상급종합병원 30% 가산율이 있음.
- 기존 급여 항목 계산은 `getBenefitChargeBase(item, hData)`에서 가산율을 이미 곱하고 있음.
- 이번 수정은 가산분을 계산 상세와 결과 요약에 별도 표시해 사용자가 확인 가능하게 만드는 작업이다.
- 검증: `node --check frontend\assets\js\script.js`, `node tests\test_runner.js`, `git diff --check` 통과.
