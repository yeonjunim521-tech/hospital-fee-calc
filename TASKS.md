# 작업 목록

## 운영 규칙

- 작업 목록 파일은 `TASKS.md` 하나만 사용한다.
- `TASKS.md`에는 현재 작업 하나만 기록한다.
- 이전 작업 기록은 `docs/TASK_HISTORY.md`에 보관한다.
- 상태 표기: `[ ]` 미완료, `[/]` 진행 중, `[x]` 완료

## 현재 작업: AdSense 승인 전 AdFit·쿠팡 배너 수익화 및 모바일 노출 개선

목표: AdSense 승인 전까지 홈 안내 영역과 정적 안내 페이지에 Kakao AdFit과 쿠팡 파트너스 다이나믹 배너를 붙이고, 모바일 계산기에서도 AdFit을 쉽게 확인할 수 있게 한다. 계산 입력·결과 화면은 광고로 가리지 않는다. 별도 worktree에서 검증한 뒤 사용자 승인에 따라 `main`에 병합·운영 배포한다.

## 작업 범위

- [x] Kakao AdFit 계정·매체·광고단위 상태 확인
- [x] 별도 worktree에서 홈·SEO 페이지 광고 배치
- [x] 실제 쿠팡 위젯 ID로 교체 후 로컬 브라우저에서 광고 렌더와 레이아웃 재확인
- [x] 버그 없음을 확인하고 메인 병합·운영 배포 승인 확보
- [x] 모바일 계산기 입력 직전에 AdFit 320×100 노출 후 반응형 회귀 검증

## 진행 메모

- AdFit 계정은 승인, 광고단위 2개는 승인, 매체 hospital-fee-calc.pages.dev는 심사 보류다. 보류 사유는 광고 설치 후 재심사 요청이다.
- 1280px 초과 PC는 왼쪽 AdFit 160×600과 오른쪽 쿠팡 120×400을 사용한다. 769~1280px는 쿠팡 680×140, 768px 이하는 쿠팡 250×250을 계산기 아래에 사용한다. AdFit 320×100은 모바일에서 계산기 입력 직전, 그보다 넓은 화면에서는 계산기 하단에 둔다.
- 쿠팡은 확인된 트래킹 코드 AF2104018과 위젯 ID 1017933·1017923·1017935를 사용한다. 공용 로더가 현재 viewport(화면 크기)에 맞는 한 개만 호출하며, 로드 실패 시 확인된 제휴 링크를 남긴다.
- 결과 보기 강제 팝업은 AdFit 정책상 넣지 않고, 결과를 먼저 계산한 뒤 즉시 닫을 수 있는 제휴 안내만 사용한다.
- 로컬 QA에서 390px 모바일 AdFit 320×100 실소재를 계산기 입력 직전에 확인했고, 계산 결과 5,100원 표시까지 통과했다. 1440px에서는 왼쪽 AdFit 160×600·오른쪽 쿠팡 120×400·하단 AdFit을 확인했다. 모든 폭에서 본문 가로 스크롤은 없었다.
- 메인 CSS에는 버전 값을 붙여 기존 방문자의 캐시 때문에 모바일 배치 규칙이 늦게 적용되지 않도록 했다.
- 사용자 후속 승인으로 메인 병합·운영 배포·운영 QA까지 진행한다. AdFit 재심사 요청은 운영 광고 확인 후 사용자가 진행한다.
- `.codex-progress/`, `.omo/`, `_tmp_*.py`는 커밋하지 않는다.
