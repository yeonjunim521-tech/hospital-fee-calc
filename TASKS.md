# 작업 목록

## 운영 규칙
- 작업 목록 파일은 `TASKS.md` 하나만 사용한다.
- `TASKS.md`에는 현재 작업 하나만 남긴다.
- 이전 작업 기록은 `docs/TASK_HISTORY.md`에 보관한다.
- 상태 표기: `[ ]` 미완료, `[/]` 진행 중, `[x]` 완료

## 현재 작업: 무결과 검색어 실제 DB 반영·주간 자동 점검

목표: 관리자 대시보드에서 검증된 의료 항목을 실제 검색 DB에 반영하고, 매주 무결과 검색어를 자동으로 후보화한다.

## 작업 범위
- [x] 승인된 의료 항목을 저장하는 D1 실제 검색 DB 추가
- [x] 관리자 승인 시 필수 가격·급여·출처 검증 후 실제 DB 반영
- [x] 공개 계산기에서 승인된 D1 항목을 검색·계산 데이터에 병합
- [x] 최근 7일 무결과 검색어를 매주 자동 후보화
- [x] 완료 후보 키워드를 기존 공식 코드 검색 별칭으로 공개
- [x] 하지 MRI 후보의 부위별 공식 코드·가격 preset 추가
- [x] 회귀 test(테스트)·브라우저 확인·원격 D1 적용·배포

## 진행 메모
- 사용자 검색어만으로 의료 항목·가격을 자동 공개하지 않는다.
- 공식 출처·가격 등 필수 근거가 없는 항목은 관리자 승인 대기에 둔다.
- 주간 자동 점검은 매주 월요일 오전 9시(KST)에 실행한다.
- 단일 확정 매칭 `중심정맥 → PR_TR09`을 완료 처리했고, 다의적 후보는 긴급 확인에 유지한다.
