# 작업 목록

## 운영 규칙
- 작업 목록 파일은 `TASKS.md` 하나만 사용한다.
- `TASKS.md`에는 현재 작업 하나만 남긴다.
- 이전 작업 기록은 `docs/TASK_HISTORY.md`에 보관한다.
- 상태 표기: `[ ]` 미완료, `[/]` 진행 중, `[x]` 완료

## 현재 작업: MEDICost 빠른 계산 사용성 개선

목표: 필수 조건 3개만으로 결과를 즉시 확인하고, 검사·치료 항목은 필요할 때만 추가한다.

## 작업 범위
- [x] 저장된 설계·현재 구현 상태 대조
- [x] 빠른 결과 직행·결과 제어 회귀 테스트
- [x] 빠른 결과 CTA·보험 설정·조건 수정 구현
- [x] 자동 테스트·Chrome 시각 검증

## 진행 메모
- 제품 표기는 `MEDICost v3.0`을 유지한다.
- 계산 공식, 의료 데이터, API, D1 schema는 변경하지 않는다.
- 기존 선택 요약·고급 항목·초기화·마취 흐름을 보존한다.
- `.codex-progress/`, `.omo/`는 커밋하지 않는다.
