# 작업 목록

## 운영 규칙

- 작업 목록 파일은 `TASKS.md` 하나만 사용한다.
- `TASKS.md`에는 현재 작업 하나만 기록한다.
- 이전 작업 기록은 `docs/TASK_HISTORY.md`에 보관한다.
- 상태 표기: `[ ]` 미완료, `[/]` 진행 중, `[x]` 완료

## 현재 작업: 관리자 통계 페이지 회귀 복구·영구 반영

목표: PC 계산기 폭 수정은 유지하면서 구형으로 돌아간 관리자 통계 화면을 최신 4탭 운영 화면으로 복구하고, 이후 Git 자동 배포에서도 다시 덮이지 않도록 원격 `main`에 영구 반영한다.

## 작업 범위

- [x] 운영 관리자 화면이 구형 버전으로 돌아간 증상 재현
- [x] 최신 관리자 변경이 운영에만 배포되고 Git `main`에는 없었던 원인 확인
- [x] 당시 검증된 관리자·익명 통계 릴리스와 최신 PC 폭 수정 병합
- [x] 전체 자동 테스트·Pages Functions 빌드·로컬 화면 검증
- [/] commit·push·PR·merge 및 Cloudflare Production 배포
- [ ] 운영 관리자 4탭·통계·버튼과 메인 PC 폭 최종 확인

## 진행 메모

- 회귀 원인: 관리자 통계 최신판은 Production 직접 배포만 됐고 원격 Git에 저장되지 않아, 후속 PC 레이아웃 Git 배포가 구형 파일로 덮었다.
- 복구 기준: 이전 Production에서 자동 테스트·Pages 빌드·실제 Chrome·자산 해시까지 확인한 격리 릴리스 복사본이다.
- PC 계산기 `.calculator-workspace` 레이아웃과 관리자 `검색 운영` 4탭을 함께 보존한다.
- 병합 브라우저 테스트에서 빈 AdFit CSS가 지연 로더까지 숨기던 회귀를 발견해, 확인된 로드 오류 상태만 접도록 수정했다.
- 운영 D1의 `visitor_daily_stats`는 이미 적용된 상태이며, 이번 복구에서 데이터 삭제·초기화·추가 migration은 하지 않는다.
- `.codex-progress/`, `.omo/`는 커밋하지 않는다.
