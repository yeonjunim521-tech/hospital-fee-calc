# 작업 목록

## 운영 규칙

- 작업 목록 파일은 `TASKS.md` 하나만 사용한다.
- `TASKS.md`에는 현재 작업 하나만 기록한다.
- 이전 작업 기록은 `docs/TASK_HISTORY.md`에 보관한다.
- 상태 표기: `[ ]` 미완료, `[/]` 진행 중, `[x]` 완료

## 현재 작업: 관리자 검색 통계 실제 검색어 복구

목표: `aggregate` 고정값 대신 개인정보 필터를 통과한 실제 검색어와 클릭 항목을 관리자 대시보드에서 확인한다.

## 작업 범위

- [x] `aggregate` 고정 저장 원인과 변경 이력 확인
- [x] 실제 검색어·검색 결과·경로 저장 복구
- [x] 클릭한 의료 항목 코드·이름 저장 복구
- [x] 주민등록번호·전화번호·이메일 패턴 차단 유지
- [x] 관리자 통계에서 기존 `aggregate` 행 제외
- [x] 전체 Node.js·TypeScript 회귀 검증
- [ ] Chrome 검증(현재 Linux 실행환경에 Chrome 실행 파일 없음)

## 진행 메모

- 검색·클릭 로그는 분석 동의가 있을 때만 전송하고 30일 뒤 삭제한다.
- IP와 User-Agent는 검색·클릭 로그에 저장하지 않는다.
- 기존 D1 컬럼을 사용하므로 schema migration은 필요하지 않다.
- `.codex-progress/`, `.omo/`는 커밋하지 않는다.
