# 작업 목록

## 운영 규칙

- 작업 목록 파일은 `TASKS.md` 하나만 사용한다.
- `TASKS.md`에는 현재 작업 하나만 기록한다.
- 이전 작업 기록은 `docs/TASK_HISTORY.md`에 보관한다.
- 상태 표기: `[ ]` 미완료, `[/]` 진행 중, `[x]` 완료

## 현재 작업: 검색 운영 관리자·익명 통계 운영 배포

목표: 미동의 사용자는 필수 조건 기본 계산을 이용하고, 개인정보 수집·이용 동의 후에만 선택 조건·추가 검색과 실제 검색어·동의 방문 통계 수집을 제공하며 C형 관리자 화면으로 운영한다.

## 작업 범위

- [x] 실제 검색어별 통계와 방문자 통계 분리 계약 확정
- [x] DESIGN.md·의사결정 기록 갱신
- [x] 검색·방문 D1 schema와 API 구현
- [x] 기본 계산·검색·선택 조건 이용 상태 구현
- [x] C형 관리자 4개 탭·삭제·처리 기능 구현
- [x] 자동 테스트·실제 브라우저·반응형 검증
- [x] 운영 D1 적용·Cloudflare Pages 배포·운영 확인
- [x] 최종 개인정보 수집·이용 동의 계약·재동의·운영 배포
- [x] 프로토타입 필수 자체통계·선택 Google Analytics 구분 및 재배포

## 진행 메모

- `aggregate` 문자열을 새 검색 기록으로 저장하지 않는다.
- 실제 검색어는 방문자 ID·IP·세션과 연결하지 않고 검색어별 횟수·미결과 횟수로 30일 보관한다.
- 익명 방문 통계는 별도 테이블에서 일간 방문자·페이지 조회만 집계한다.
- 미동의 사용자는 필수 조건 기본 계산만 가능하며 검색·선택 조건은 잠근다.
- 개인정보 수집·이용 동의 화면에는 목적·항목·30일 보유기간·거부 권리와 기능 제한을 표시하고 `medicost-consent-v3`로 다시 선택받는다.
- 추가 완료 삭제는 처리 이력만 삭제하고 공개 의료수가 항목은 유지한다.
- 운영 QA에서 `mri` 검색은 정확히 1회 증가했고, 방문 통계는 별도 일별 행으로 생성됐으며 기존 `aggregate` 행 수는 증가하지 않았다.
- Cloudflare Pages, 원격 D1 방문 통계 schema, 30일 보유기간 정리 Worker 배포와 운영 URL 확인을 완료했다.
- 최종 동의 계약은 `medicost-consent-v3`로 배포했고 기존 선택은 자동 승계하지 않는다.
- 자체 검색어·방문 통계는 프로토타입 선택 기능 이용 시 필수이며, Google Analytics는 별도 선택으로 유지한다.
- 운영 미동의 QA에서 기본 계산 후 계산 로그 `7→7`, 방문 통계 `2→2`로 전송이 없었고, 동의 후 방문 통계 `2→3`, `mri` 실제 검색 `5→6`으로 각각 1건 증가했다. 기존 `aggregate` 행은 35건으로 유지됐다.
- 최종 Pages 배포 미리보기는 `https://cb335bd5.hospital-fee-calc.pages.dev`, 운영 URL은 `https://hospital-fee-calc.pages.dev/#calculator`다.
- 운영 화면에서 `[필수]` 자체 방문·검색 통계와 `[선택]` Google Analytics 표기, 미동의 기본 계산, 동의 후 검색 활성화, Google Analytics 비자동선택을 확인했다.
- 기존 미커밋 `frontend/index.html`·브라우저 테스트 변경 중 최신 요구와 충돌하지 않는 부분은 보존한다.
- `.codex-progress/`, `.omo/`는 커밋하지 않는다.
