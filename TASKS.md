# 작업 목록 운영 규칙

앞으로 기능 추가, 수정, 검증 작업을 시작하기 전에는 이 파일에 작업 목록을 먼저 작성하거나 갱신한다.

## 진행 방식

- 작업 전 `TASKS.md`에 목표, 범위, 체크리스트를 작성한다.
- 사용자가 작업 목록을 확인한 뒤 진행한다.
- 진행 중에는 완료된 항목을 체크하거나 상태를 갱신한다.
- 작업이 끝나면 검증 결과와 남은 후속 작업을 기록한다.

## 현재 작업 예정: 응급실 중증도 선택 기능

목표: 응급실 진료를 단일 비용으로 계산하지 않고, 경증·중등도·중증 등 중증도에 따라 예상 응급실 비용이 달라지게 한다.

## 작업 범위

- [x] 현재 응급실 계산 로직과 `ER_*` 항목 구조 확인
- [x] 응급실 선택 시 중증도 선택 UI 추가
- [x] 경증·중등도·중증별 응급의료관리료 또는 가산 비용 계산 반영
- [x] 기존 응급실 항목과 새 중증도 선택이 중복 계산되지 않게 정리
- [x] 결과 명세서에 응급실 중증도 항목 표시
- [x] 외래·입원 계산에는 영향 없는지 검증
- [x] `node --check script.js` 실행

## 검증 결과

- `node --check script.js` 통과
- 상급종합 기준 기본 시나리오 확인
  - 일반 외래: 13,800원
  - 응급실 경증: 25,800원
  - 응급실 중등도: 49,800원
  - 응급실 중증: 85,800원
  - 입원 1일: 35,200원

## 다음 작업 예정: 비급여 지역별 평균 데이터 조사

목표: 비급여 항목을 단일 금액이나 임의 배율로 계산하기 전에, 공식 또는 신뢰 가능한 지역별 평균 데이터가 있는지 확인한다.

## 작업 범위

- [x] 심평원 비급여 진료비 공개 서비스에 지역별 평균/중앙값 데이터가 있는지 확인
- [x] 공개 데이터가 내려받기 가능한 형태인지 확인
- [x] 지역 단위가 시도/시군구/기관별 중 어디까지 제공되는지 확인
- [x] 앱에 적용 가능한 데이터 구조 제안
- [ ] 구현 여부는 조사 결과 확인 후 결정

## 확인 필요

- [x] 기존 구현 범위 보류
- [x] 지역별 평균 데이터 조사 결과 확인

## 조사 결과

- 공식 OpenAPI에 `비급여진료비용지역별정보` 기능 존재
- 제공 필드: 비급여코드, 적용시작일, 전체 최대비용, 지역별 최대/최소/평균/중간비용
- 기관별 상세 조회는 시도·시군구·종별·병원명 조건 지원
- API 데이터 포맷은 XML, 자동승인, 무료, 개발계정 기본 트래픽 10,000건
- 앱 적용 시 필요 작업: HIRA 비급여 코드와 현재 `hira_codes.js` 내부 항목 코드 매핑, API 키 설정, 지역 선택 UI 추가

## 현재 작업 예정: API 키 없는 비급여 지역 평균 적용

목표: API 키 없이도 최신 공개자료 기준의 지역별 비급여 평균값을 앱 계산에 반영한다.

## 작업 범위

- [x] 오늘 기준 최신 공개자료 출처 확인
- [x] API 없이 사용할 정적 공개 비급여 데이터 구조 설계
- [x] 대표 비급여 항목부터 건강보험공단 비급여 항목코드 매핑
- [x] 지역 선택 UI 추가
- [x] 비급여 항목 계산 시 공개 중앙값 우선 적용
- [x] 데이터가 없는 비급여 항목은 기존 가격 fallback
- [x] 결과 명세서에 공개 중앙값 적용 여부 표시
- [x] 검증 및 `TASKS.md` 결과 갱신

## 확인 필요

- [x] 위 범위로 진행 승인

## 진행 메모

- API 키 없이 확인 가능한 공식 화면 내부 조회로 대표 비급여 항목 가격을 확인했다.
- 공식 비교 조회는 평균값보다 중앙값과 최저값을 주로 반환했다.
- 확인된 대표 항목:
  - 도수치료 `MX1220000`: 중앙 100,000원, 최저 30,000원
  - 슬관절 MRI 3차원 `HE5200000`: 중앙 550,000원, 최저 400,000원
  - 발목관절 MRI 3차원 `HE5210000`: 중앙 560,000원, 최저 400,000원
  - 견관절 MRI 3차원 `HE5150000`: 중앙 550,000원, 최저 400,000원
  - 고관절 MRI 3차원 `HE5180000`: 중앙 594,900원, 최저 400,000원
  - 관절외 하지 MRI 3차원 `HE5230000`: 중앙 555,790원, 최저 400,000원
- 지역별 상세 데이터셋 경로는 확인했지만, 현재 직접 요청은 빈 응답 또는 오류라 앱에는 허위 지역별 금액을 넣지 않는다.

## 검증 결과

- `node --check script.js` 통과
- 스모크 테스트 통과
  - 도수치료 `PR_RE04`는 기존 110,000원 대신 공식 공개 중앙값 100,000원 적용
  - 슬관절 MRI `IM_MR05`는 기존 400,000원 대신 공식 공개 중앙값 550,000원 적용
  - 미매핑 비급여 항목은 기존 앱 추정가 fallback 유지

## 남은 후속 작업

- 지역별 상세 평균·중앙값 자동 반영은 공식 OpenAPI 키 또는 안정적인 지역별 데이터셋 호출 확보 후 진행한다.

## 현재 작업 예정: 수집된 비급여 지역별 데이터 앱 반영

목표: 프로젝트 폴더에 추가된 `nonbenefit_region_prices.json`, `nonbenefit_code_map.json`을 읽어 비급여 지역별 평균·중앙값을 계산에 자동 반영한다.

## 작업 범위

- [ ] 신규 JSON 파일 구조 확인
- [ ] 앱 내부 비급여 코드와 공식 비급여 코드 매핑 방식 확정
- [ ] 지역 선택 UI를 실제 수집 지역 목록으로 자동 구성
- [ ] 계산 시 선택 지역의 중앙값 우선, 평균값 보조, 기존 가격 fallback 적용
- [ ] 명세서에 적용 기준(지역·중앙값/평균값·공식 코드) 표시
- [ ] 데이터 로드 실패 시 기존 정적 fallback 유지
- [ ] 문법 및 계산 스모크 테스트
- [ ] 결과를 `TASKS.md`에 기록

## 현재 작업 진행: 깃 초기화 및 깃허브 비공개 원격 저장소 생성

목표: 현재 프로젝트 폴더를 깃 저장소로 초기화하고, 깃허브에 비공개 원격 저장소를 만든 뒤 `origin` 리모트로 연결한다.

## 작업 범위

- [x] 현재 폴더의 깃 상태 확인
- [x] 필요 시 `.gitignore` 구성
- [x] `git init` 실행
- [x] 깃허브 인증 및 명령 도구 사용 가능 여부 확인
- [ ] 깃허브 비공개 원격 저장소 생성
- [ ] 로컬 저장소에 `origin` 리모트 연결 확인
- [x] 결과를 `TASKS.md`에 기록

## 진행 결과

- 로컬 저장소는 `main` 브랜치로 초기화 완료.
- `chrome_dev/`, `edge_dev_temp/`, 로그, 임시 파일, 환경 파일을 제외하는 `.gitignore` 추가.
- 현재 환경에는 `gh` 명령 도구가 없고 `GITHUB_TOKEN`/`GH_TOKEN`도 없어 비공개 깃허브 저장소 생성은 인증 정보 확보 후 진행 필요.

## 현재 작업 완료: 백엔드 자동 최신화 대비 코드 정리 및 개발중 안내 추가

목표: 차후 실시간 데이터베이스와 백엔드 API를 붙일 수 있도록 데이터 로딩 구조를 정리하고, 현재 화면에 개발중·지속 업데이트 안내를 추가한다.

## 작업 범위

- [x] 현재 정적 비급여 데이터 로딩 구조 확인
- [x] 백엔드 API 우선, 정적 파일 fallback 데이터 로딩 구조 추가
- [x] 향후 백엔드 API 계약과 샘플 서버 파일 추가
- [x] 화면 상단에 개발중·지속 업데이트 안내 추가
- [x] 문법 및 기본 동작 검증
- [x] 결과를 `TASKS.md`에 기록

## 진행 결과

- `script.js`에 데이터 소스 초기화 계층을 추가했다. 같은 출처의 `/api/nonbenefit/region-prices`, `/api/nonbenefit/code-map`이 응답하면 백엔드 최신자료를 우선 사용하고, 실패하면 기존 `nonbenefit_data.js` 정적 자료를 사용한다.
- 화면 헤더에 개발중·지속 업데이트 안내 박스와 데이터 소스 상태 문구를 추가했다.
- `backend/server.js`를 추가해 정적 계산기와 비급여 API를 함께 제공하는 최소 백엔드 골격을 만들었다.
- `backend/README.md`에 향후 데이터베이스 전환 시 유지해야 할 API 응답 형태를 기록했다.
- 검증:
  - `node --check script.js` 통과
  - `node --check backend\server.js` 통과
  - 임시 백엔드 실행 후 `/api/health`, `/api/nonbenefit/region-prices` 200 응답 확인

## 현재 작업 진행: 깃허브 비공개 업로드 및 자동 연동 준비

목표: 프로젝트를 깃허브 비공개 저장소에 올리고, 이후 로컬 업데이트가 깃허브에 자동 반영되도록 준비한다.

## 작업 범위

- [x] 현재 깃 원격 저장소 상태 확인
- [x] 깃허브 인증 수단 확인
- [x] 비공개 원격 저장소 생성 가능 여부 확인
- [x] 자동 커밋·푸시 스크립트 추가
- [x] 커밋 후 자동 푸시 훅 구성
- [ ] 실제 원격 연결 및 첫 업로드
- [x] 결과를 `TASKS.md`에 기록

## 진행 결과

- 현재 `origin` 리모트 없음.
- 현재 환경에 `gh` 명령 도구 없음.
- `GH_TOKEN`/`GITHUB_TOKEN` 없음. 그래서 깃허브 비공개 저장소 생성과 첫 업로드는 인증 정보 없어서 보류.
- `scripts/create-private-github-repo.ps1` 추가: 토큰이 있으면 비공개 저장소 생성 및 `origin` 연결.
- `scripts/sync-github.ps1` 추가: 변경사항 자동 커밋 후 `origin/main` 푸시.
- `scripts/install-auto-push-hook.ps1` 추가 및 `.git/hooks/post-commit` 설치 완료: 커밋마다 자동 푸시. 단, `origin` 없으면 건너뜀.
- 첫 로컬 커밋 생성 완료: `768cb9b chore: initial project setup`.

## 현재 작업 완료: 제공된 깃허브 토큰으로 비공개 저장소 생성 및 첫 업로드

목표: 제공된 깃허브 토큰을 임시 환경 변수로만 사용해 비공개 저장소를 만들고, `origin/main`에 현재 프로젝트를 업로드한다.

## 작업 범위

- [x] 토큰을 파일에 저장하지 않고 현재 명령 환경에서만 사용
- [x] 비공개 깃허브 저장소 생성
- [x] `origin` 리모트 연결
- [x] 현재 커밋 푸시
- [x] 자동 푸시 스크립트가 토큰 기반 푸시를 지원하도록 보완
- [x] 결과를 `TASKS.md`에 기록

## 진행 결과

- 사용자 명시 승인 후 깃허브 비공개 저장소를 생성했다.
- 원격 저장소: `https://github.com/yeonjunim521-tech/hospital-fee-calc`
- `origin/main` 연결 및 첫 푸시 완료.
- 커밋 후 자동 푸시 훅은 설치 완료.
- `scripts/sync-github.ps1`와 훅은 `GH_TOKEN` 또는 `GITHUB_TOKEN`이 현재 실행 환경에 있을 때 토큰 기반 푸시를 지원한다.
- 제공된 토큰은 파일에 저장하지 않았다.
- 토큰은 대화에 노출됐으므로 깃허브에서 폐기·재발급하는 것이 안전하다.

## 현재 작업 완료: 검색 노출 및 클라우드플레어 페이지 배포 준비

목표: 구글·네이버 검색 노출과 빠른 정적 웹앱 배포를 위해 SEO 파일, 안내 페이지, 배포 문서를 추가한다.

## 작업 범위

- [x] 배포 기준 주소 설정
- [x] `robots.txt` 추가
- [x] `sitemap.xml` 추가
- [x] `ads.txt` 자리 파일 추가
- [x] 개인정보처리방침/문의/소개 페이지 추가
- [x] 병원비·응급실 비용 검색용 랜딩 페이지 추가
- [x] 메인 페이지 검색 메타 태그 보강
- [x] 클라우드플레어 페이지 배포 문서 추가
- [x] 검증 및 결과 기록

## 진행 결과

- 예상 배포 주소를 `https://hospital-fee-calc.pages.dev/`로 설정했다.
- 검색 노출용 파일 추가: `robots.txt`, `sitemap.xml`.
- 광고 준비용 `ads.txt` 자리 파일 추가. 광고 승인 후 실제 게시자 값으로 교체 필요.
- 검색 랜딩 페이지 추가:
  - `hospital-cost-calculator.html`
  - `er-cost-calculator.html`
- 신뢰도·광고 심사 준비 페이지 추가:
  - `about.html`
  - `privacy.html`
  - `contact.html`
- 클라우드플레어 페이지 설정 문서 `DEPLOY.md` 추가.
- 검증: `node --check script.js` 통과.
- 깃허브 플러그인은 재인증 필요 상태라 원격 반영은 보류. 로컬 커밋 후 재인증 또는 새 토큰으로 푸시 필요.

## 현재 작업 완료: 구글 사이트 소유권 인증 파일 추가

목표: 구글 서치 콘솔에서 요구한 HTML 인증 파일을 웹앱 루트에 배치하고 배포 저장소에 반영한다.

## 작업 범위

- [x] 다운로드 폴더의 인증 파일 확인
- [x] 프로젝트 루트에 인증 파일 추가
- [x] 깃 커밋 및 깃허브 반영
- [x] 인증 파일 접근 경로 안내

## 진행 결과

- `google846c818f5952f2aa.html` 파일을 프로젝트 루트에 추가했다.
- 커밋 및 깃허브 푸시 완료: `3d10cb1 chore: add google site verification file`.
- 배포 후 확인 경로: `https://hospital-fee-calc.pages.dev/google846c818f5952f2aa.html`

## 현재 작업 완료: 네이버 사이트 소유권 인증 파일 추가

목표: 네이버 서치어드바이저에서 요구한 HTML 인증 파일을 웹앱 루트에 배치하고 배포 저장소에 반영한다.

## 작업 범위

- [x] 다운로드 폴더의 인증 파일 확인
- [x] 프로젝트 루트에 인증 파일 추가
- [x] 깃 커밋 및 깃허브 반영
- [x] 인증 파일 접근 경로 안내

## 진행 결과

- `naver9833def883434392c485818bfb777010.html` 파일을 프로젝트 루트에 추가했다.
- 커밋 및 깃허브 푸시 완료: `6846e8a chore: add naver site verification file`.
- 배포 후 확인 경로: `https://hospital-fee-calc.pages.dev/naver9833def883434392c485818bfb777010.html`
