# Local fonts

- `outfit-latin.woff2`: Outfit variable Latin subset, used for monetary figures and version labels.
- Korean body text uses the locally installed `Noto Sans KR` when available and falls back to `Malgun Gothic`.
